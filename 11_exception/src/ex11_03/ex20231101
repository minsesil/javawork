/*
 * NumberMenu
+ menu() : void
   설명 : 사용자에게 두 정수를 입력 받아 NumberController의 check()에 
	매개변수로 넘겨주어 반환 값을 출력함
	 이 때 checkDouble()메소드를 호출하는 부분부터 try~catch를 이용하여 예외 처리
	 
 * NumberController
+ NumberController()
+ check(num1:int, num2:int) : boolean throws NumRangeException
   설명 : 매개변수로 들어온 두 값이 1~100 사이 일 때 num1이 num2의 배수면 true,  
	 배수가 아니라면 false 반환
	 1~100사이가 아니라면 NumRangeException발생, 에러 메시지는 출력 값 참고

출력(1~100사이값만 입력할 수 있음)
정수1 : 55
정수2 : 5
55은(는) 5의 배수인가 ? true 

또는

정수1 : 19
정수2 : 7
19은(는) 7의 배수인가 ? false 

또는

정수1 : 104
정수2 : 2
예외 처리 문구 나옴
 */